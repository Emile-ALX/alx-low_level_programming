#include <stdio.h>
#include <string.h>

/**
 * main - a function that concatenates two strings
 *
 * Return: concatenated strings
 */
int main(void)
{
	char *dest;
	char *src;

	puts(dest, src);
	return (0);
}
